# 公司最新版 2026-08-07 V70

## 內容

- `outputs/物件管理總表_單機版.html`：V70 單機版。
- `app/`、`scripts/`、`public/` 等：完整原始程式與建置檔案。
- `物件總表_最新資料.json` 與 `outputs/*.json`：目前攜帶的 JSON 資料。
- `需求對話原文.md`：先前需求對話紀錄。
- `.git/`：從 V70 開始建立的 Git 修改歷程。
- `V70完整Git紀錄.bundle`：可在另一台電腦還原 Git 專案的單一備份檔。

## 到公司後

1. 將整個資料夾複製到公司電腦。
2. 要直接使用時，開啟 `outputs/物件管理總表_單機版.html`。
3. 要繼續修改時，把本資料夾路徑告訴 Codex。
4. `node_modules` 未附入；需要重新建置時，在有 Node.js 的環境執行 `npm install`。

## Git 說明

原來的 V31/V70 工作資料夾沒有 `.git`，因此沒有早期修改歷程可以復原。本移轉包已從 V70 完整狀態建立第一筆 Git 紀錄，之後的修改可正常追蹤。
